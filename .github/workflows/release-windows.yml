name: "release-windows"

on:
  workflow_call:
    inputs:
      release_tag:
        required: true
        type: string
      version:
        required: true
        type: string

jobs:
  noop:
    name: Windows builds disabled
    runs-on: windows-latest
    steps:
      - name: No-op
        run: echo "Windows builds are currently disabled"
