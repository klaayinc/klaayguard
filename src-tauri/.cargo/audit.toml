[advisories]
# Keep this list empty unless an advisory is genuinely unfixable. Every entry
# must name the crate path that pulls the vulnerable version in and the
# condition under which it can be dropped, and must be re-checked whenever
# Cargo.lock changes: a stale ignore silently masks a future regression.
ignore = []
